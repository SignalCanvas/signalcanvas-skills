Run the SignalCanvas signal flow protocol test suite and report results.

Execute: `npx vitest run src/tests/signalFlow/`

Report how many tests passed, how many failed, and if any failed, show which test names failed and the assertion errors.
